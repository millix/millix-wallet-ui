export const SHOW_API_ERROR_IN_MODAL = false;

export const NODE_ID        = '';  // to be used in src/js/api/index.js
export const NODE_SIGNATURE = '';  // to be used in src/js/api/index.js

const environment = {
    SHOW_API_ERROR_IN_MODAL,
    NODE_ID,
    NODE_SIGNATURE
};

export default environment;
